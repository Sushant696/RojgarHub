function AuthModal() {
  return (
    <div>
      If users tries to do something that he is not alowed to then he will get
      this modal where he can login in with appropriate access account or
      contiue using it
    </div>
  );
}

export default AuthModal;
